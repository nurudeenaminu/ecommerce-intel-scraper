New-Item -ItemType Directory -Force -Path .streamlit
@"
GEMINI_API_KEY = REMOVED_KEY" | Out-File -FilePath replacements.txt -Encoding utf8"
"@ | Out-File -FilePath .streamlit/secrets.toml -Encoding utf8